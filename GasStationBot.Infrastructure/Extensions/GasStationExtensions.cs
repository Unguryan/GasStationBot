namespace GasStationBot.Infrastructure.Extensions
{
    public static class GasStationExtensions
    {
        //public static Dictionary<FuelType, FuelState> CheckUpdatedFuels(this IEnumerable<Fuel> fuels,
        //                                                                Dictionary<FuelType, List<FuelState>> userFuels)
        //{
        //    var result = new Dictionary<FuelType, FuelState>();
        //    foreach (var userFuel in userFuels)
        //    {
        //        var fuel = fuels.FirstOrDefault(f => f.FuelType == userFuel.Key);

        //        if(fuel == null)
        //        {
        //            continue;
        //        }

        //        foreach (var userFuelState in userFuel.Value)
        //        {

        //        }
        //    }
        //}
    }
}
